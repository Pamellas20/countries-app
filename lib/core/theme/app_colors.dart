import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  // Light mode colors
  static const Color lightBackground = Color(0xFFFFFFFF);
  static const Color lightPrimaryText = Color(0xFF121417);
  static const Color lightSecondaryText = Color(0xFF6B7582);
  static const Color favoriteColor = Colors.red;

  // Dark mode colors
  static const Color darkBackground = Color(0xFF121417);
  static const Color darkPrimaryText = Color(0xFFFFFFFF);
  static const Color darkSecondaryText = Color(0xFF9CA3AF);
}
